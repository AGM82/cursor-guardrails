---
description: Project-specific context — fill this in for every project
alwaysApply: true
---

# Project: <PROJECT NAME>

> This is the one file you customise for every project. Replace every <PLACEHOLDER> and delete this line.

## What this is
<One paragraph: what the app does, who uses it, and the core workflow.>

## Domain glossary
<Define the domain terms the agent must use correctly, e.g. endorsement, schedule, peril, excess, sum insured, broker.>

## Tech stack
Default: React + TypeScript + Tailwind + shadcn/ui + Recharts. Deviations for this project:
<List any departures from the default, or "None.">

## Canonical files (reference these — do not reinvent)
- Component: <path to the reference component>
- API route / data access: <path>
- Test: <path>

## Architecture decisions
<State management, data layer, auth approach, routing — anything load-bearing.>

## Data classification (POPIA)
- Personal-information categories this app touches: <e.g. name, contact details, policy number — or "None; templated data only">
- Retention / access constraints: <state them, or "N/A for templated data">

## Out of scope / do not touch
<Areas the agent must not modify without explicit instruction.>
